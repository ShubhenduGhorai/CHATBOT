# chatbot

Production-ready multi-tenant SaaS starter with Next.js App Router, TypeScript, Tailwind CSS, Prisma, and PostgreSQL.

## Quick start

```bash
npm install
cp .env.example .env
npx prisma migrate dev --name init
npm run dev
```
